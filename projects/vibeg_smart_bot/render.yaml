services:
  - type: web
    name: vibegnews-telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    pythonVersion: 3.10
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: MISTRAL_API_KEY
        sync: false

